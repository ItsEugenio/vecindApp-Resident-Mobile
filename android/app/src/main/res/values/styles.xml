<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- ✅ Tema aplicado a la ventana de Android mientras la app se inicia -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Mostrar la pantalla de lanzamiento mientras Flutter carga -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <!-- ✅ Tema principal de la app -->
    <style name="NormalTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>

    <!-- ✅ Soporte para Firebase Services -->
    <string name="default_web_client_id">29247945160-xxxxxxxxxxxxxxx.apps.googleusercontent.com</string>

    <!-- ✅ Notificaciones de Firebase -->
    <string name="default_notification_channel_id">default_channel</string>

</resources>
